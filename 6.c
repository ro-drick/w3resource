#include <stdio.h>

int main(){
    float radius, perimeter, area;
    radius = 6;
    perimeter = 2 * 3.14 * radius;
    area = 3.14 * radius * radius;
    printf("Perimeter: %f\n", perimeter);
    printf("Area: %f", area);

    return 0;
}
